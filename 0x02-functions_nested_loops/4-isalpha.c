#include <main.h>

/**
 * main - function that chcks for alphabetic character 
 *
 * Returns:1 if c is a letter,lowercase or uppercase
 * and  othewise
 */
int _isalpha(int c)
{

if (c > 'a' && c < 'z')
{
if (c > 'A' && C < 'Z')
{
return (1);
}

}
else
{
return (0);
}

}
