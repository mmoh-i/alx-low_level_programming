#include "main.h"
/**
 * print_prev - prints a string and a new line
 *
 * Return : a new line
 */
void print_rev(char *s)
{
	int i = 0;
	length;
	
	for (i = 0, s[length] != '\0'; legth++)
	{
	}

	for (i = length -1; i >= 0; i--)
	{
		_putchar(s[i])
	}
	_putchar('\n');
}
